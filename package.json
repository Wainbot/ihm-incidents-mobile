{
  "name": "red-sound",
  "version": "1.0.0",
  "description": "My app description",
  "scripts": {
    "deploy": "bower install && npm test && git push heroku master",
    "start": "node server.js",
    "postinstall": "bower install"
  },
  "dependencies": {
    "express": "^4.13.4",
    "express-validator": "^2.20.4"
  },
  "devDependencies": {},
  "engines": {
    "node": "6.1.0"
  }
}